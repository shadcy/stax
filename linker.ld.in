/* ============================================================================
 * TIOS — linker.ld
 * GNU ld linker script for the ARM Versatile Platform Baseboard (versatilepb).
 * ============================================================================ */

#include "memory_map.h"

/* Entry point: _start is defined in startup.s */
ENTRY(_start)

/* --------------------------------------------
 * MEMORY — declare the physical regions available to the linker.
 * -------------------------------------------- */
MEMORY
{
    RAM (rwx) : ORIGIN = KERNEL_EXEC_ADDR, LENGTH = 32M
}

/* --------------------------------------------
 * SECTIONS — describe how to pack input sections into output sections
 * -------------------------------------------- */
SECTIONS
{
    /* ---- Code and read-only data ---- */
    .text : {
        _text_start = .;
        *(.text._start)         /* startup.s _start FIRST */
        *(.text*)               /* all other .text input sections */
        *(.rodata*)             /* read-only data */
        *(.glue_7)
        *(.glue_7t)
        *(.vfp11_veneer)
        *(.v4_bx)
        . = ALIGN(4);
        _text_end = .;
    } > RAM

    /* ---- Initialised data ---- */
    .data : {
        . = ALIGN(4);
        _data_start = .;
        *(.data*)
        . = ALIGN(4);
        _data_end = .;
    } > RAM

    /* ---- Uninitialised data (BSS) ---- */
    .bss : {
        . = ALIGN(4);
        __bss_start = .;
        *(.bss*)
        *(COMMON)
        . = ALIGN(4);
        __bss_end = .;
    } > RAM

    /* ---- Stack (allocated BEFORE heap, grows downward) ---- */
    . = ALIGN(8);
    . += 32768;               /* 32 KB stack space */
    stack_top = .;

    /* ---- Heap (takes up the rest of the 32MB RAM) ---- */
    . = ALIGN(4096);          /* Page aligned */
    __heap_start = .;

    /* ---- Discard debug sections ---- */
    /DISCARD/ : {
        *(.ARM.exidx*)
        *(.comment)
    }
}
